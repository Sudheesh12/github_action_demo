name: My First CI Pipeline

on:
  push:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        
      - name: Run a simple command
        run: echo "wow the pipeline is working"

      - name: Run python Code
        run: python app.py
  
